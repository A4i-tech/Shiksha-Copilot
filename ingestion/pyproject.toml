[project]
name = "shiksha-ingestion-scripts"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "omni-ingest>=1.0",
    "aiohttp>=3.9",
    "pyyaml>=6.0",
    "pydantic>=2.0",
    "openpyxl>=3.1",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.21",
]
