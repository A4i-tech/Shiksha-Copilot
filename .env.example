# Data stack (docker-compose.data.yml) credentials.
# Real values live only in .env on the UAT host — never committed.
# Docker Compose auto-loads .env from this directory for any -f invocation here.

MONGO_ROOT_USER=
MONGO_ROOT_PASSWORD=
