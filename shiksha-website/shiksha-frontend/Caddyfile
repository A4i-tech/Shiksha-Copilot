:8000 {
	root * /srv
	file_server

	# Runtime config, rewritten by docker-entrypoint.sh on every container
	# start. Serving a cached copy would pin the app to a previous deploy's
	# backend URL or Turnstile key.
	@env_js path /assets/env.js
	header @env_js Cache-Control "no-store"
}
